"""
widgets — small, reusable Qt widgets shared across panels (folder/image
pickers, the progress bar + status label, the log view). No package-level
re-exports here — each widget is imported directly by module, e.g.
``from pyS3M.gui.widgets.folder_picker import FolderPicker``.
"""
